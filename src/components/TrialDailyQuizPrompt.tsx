/**
 * Previously auto-prompted today's Daily Quiz after login and routed into
 * `/iq-rank-boost-quiz`, whose back target was the Quiz hub.
 * Disabled so students stay on Home / Exams unless they open Quiz themselves.
 */
export default function TrialDailyQuizPrompt() {
  return null;
}
